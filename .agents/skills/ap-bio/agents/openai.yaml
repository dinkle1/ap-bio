interface:
  display_name: "Ap Bio"
  short_description: "Repo-specific patterns and workflows for ap-bio"
  default_prompt: "Use the ap-bio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true